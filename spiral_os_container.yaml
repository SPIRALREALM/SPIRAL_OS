image: nvidia/cuda:11.8.0-cudnn8-runtime-ubuntu22.04
packages:
  - git
  - ffmpeg
  - build-essential
  - libsndfile1
copy:
  - requirements.txt
run:
  - pip install --no-cache-dir -r requirements.txt
  - pip install --no-cache-dir qutip pyOpenSSL cryptography
  - mkdir -p INANNA_AI GENESIS IGNITION QNL_LANGUAGE
cmd: ["bash"]
